{
  "timeoutSec": 15,
  "successRegex": [
    "Started.*Application",
    "Server: Implementation"
  ],
  "requiredEnv": [
    "OPENAI_API_KEY"
  ],
  "aiValidation": {
    "enabled": true,
    "validationMode": "hybrid",
    "readmeFile": "../README.md",
    "expectedBehavior": "MCP server should start successfully and initialize with weather forecast tools. The server exposes an HTTP endpoint at /updateTools for triggering dynamic tool updates and maintains a MCP server that can add math tools on demand.",
    "promptTemplate": "example_validation",
    "successCriteria": {
      "expectedComponents": ["spring_boot_startup", "mcp_server_initialization", "weather_tools_loaded", "web_server_ready"],
      "validationNote": "Tests MCP server readiness for dynamic tool management"
    }
  }
}